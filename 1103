#include<stdio.h>
int main()
{
    int i,j,a,b;
    for(i=0 ; i<100 ; i++){
        scanf("%d %d",&a,&b);

        if(a>b)
            printf("Decrescente\n");
        if(a<b)
            printf("Crescente\n");
        if(a==b)
            return 0;
    }

    return 0;
}
