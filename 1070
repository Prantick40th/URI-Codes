#include<stdio.h>

int main()
{
    int i,n,cnt=0;

    scanf("%d",&n);

    for(i=n; ;i++)
    {
        if(i%2==1)
        {
            cnt++;

            if(cnt>6)
                break;
            else
                printf("%d\n",i);
        }
    }

    return 0;
}
