#include <stdio.h>
int main()
{
    float arr[5];
    int i,cnt;
    int a = 0;
    cnt = 0;

    for(i = 0 ; i < 6 ; i++){
        scanf("%f",&arr[i]);
    }

    for(i = 0 ; i < 6 ; i++){
        if (arr[i] > 0){
            cnt++;
        }
        else a = a + 1;
    }

    printf("%d valores positivos\n",cnt);

    return 0;
}
